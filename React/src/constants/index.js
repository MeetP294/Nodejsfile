// @TODO: Create new field on Drupal Group entity type called lead_time
// ASAP_PICKUP_TIME_OFFSET_MINUTES should be a variable set by the incoming venue lead_time
// So that each venue can set their lead time independently
export const ASAP_PICKUP_TIME_OFFSET_MINUTES = 45;
